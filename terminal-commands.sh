# Stage all changes
git add .

# Commit the changes with a descriptive message
git commit -m "Update navbar: remove logo, add favicon, reduce typed text size"

# Push changes to the remote repository
git push